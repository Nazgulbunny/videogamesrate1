<div class="row" id="user-home">
  <div id="user-info-container" class="col-lg-2">
    <%= render 'shared/user_info' %>
  </div>
  <div id="newsfeed" class="col-lg-7">
    <%= render 'posts/form' %>
    <br>
    <div id="activities">
      <% if @activities.empty? %>
        <div class="well">
          <%= render('shared/no_resource', resources: 'Activities') %>
        </div>
      <% else %>
        <%= render_activities(@activities, layout: '/shared/activity') %>
      <% end %>
    </div>
    <%= render 'shared/paginate', resources: @activities %>
  </div>
  <div class="col-lg-3">
    <%= render 'shared/links' %>
  </div>
</div>